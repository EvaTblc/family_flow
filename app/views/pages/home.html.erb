<h1>HOME</h1>
<%= link_to "Déconnexion", destroy_user_session_path, data: {turbo_method: :delete, turbo_confirm: "Vous voulez vraiment partir ?"} if user_signed_in? %>
<%= link_to "Index tasks", tasks_path %>

<% if @organization %>
  <%= link_to "My Org", organization_path(@organization) %>
<% end %>
