<div class="top_page-pixl">
  <div class="welcome">
    <div class="user-welcome">
      <%= link_to profile_user_path(current_user) do %>
        <%= cl_image_tag current_user.avatar.key, class: "avatar" %>
      <% end %>
    </div>
    <h2 ><%= current_user.username %></h2>
    <h4><%= current_user.first_name %></h4>
    <h4><%= current_user.last_name %></h4>
    <h5><%= current_user.email %></h4>
  </div>
</div>
<div class="maincontainer">

  </div>
</div>
  <div class="links_profile">
    <%= link_to "Déconnexion", destroy_user_session_path, data: {turbo_method: :delete, turbo_confirm: "Vous voulez vraiment partir ?"} %>
    <%= link_to "EDIT", edit_user_registration_path %>
  </div>

  <div class="tasks_profile">
    <% current_user.tasks.each do |task| %>
      <%= task.title %>
      <%= task.category %>
      <%= task.start %>
      <%= task.end %>
    <% end %>
  </div>
</div>
